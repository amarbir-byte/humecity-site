---
interface Props {
  title: string;
  description: string;
  href: string;
  icon: string;
}

const { title, description, href, icon } = Astro.props;
---
<a href={href} class="group block bg-white rounded-xl shadow-md hover:shadow-xl transition-all duration-300 p-6 border border-gray-100 hover:border-accent">
  <div class="text-4xl mb-4">{icon}</div>
  <h3 class="text-lg font-bold text-primary group-hover:text-accent transition-colors mb-2">{title}</h3>
  <p class="text-gray-600 text-sm leading-relaxed">{description}</p>
  <span class="inline-block mt-4 text-accent font-semibold text-sm group-hover:translate-x-1 transition-transform">
    Learn more &rarr;
  </span>
</a>
